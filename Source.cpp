#include <iostream>
#include "fraction.h"
void Fraction::print()
{
	std::cout << numerator << '/' << denominator << std::endl;
}
FractFraction::SumFraction(Fraction f1, Fraction f2);
Fraction sum;
int main()
{
	Fraction f;
	f.numerator = (2, 3);
	f.denominator = (1, 3);
	std::cout << f.numerator << '/' <<f.denominator << std::endl;
	return 0;
}